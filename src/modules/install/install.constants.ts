export type InstallSettingsView = {
  country: string;
  currency: string;
  locale: string;
  timezone: string;
  phoneRegion: string;
  taxIdLabel: string;
  taxRate: number;
  setupCompletedAt: string | null;
};
